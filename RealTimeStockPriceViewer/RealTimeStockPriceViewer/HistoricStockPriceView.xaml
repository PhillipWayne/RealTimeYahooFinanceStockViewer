<Window x:Class="RealTimeStockPriceViewer.HistoricStockPriceView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:RealTimeStockPriceViewer.ViewModels"
        Title ="Historic Stock Prices" Width="867">
    <Window.DataContext>
        <viewModels:HistoricStockPriceViewModel/>
    </Window.DataContext>
    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label FontWeight="Bold" Margin="5,5,5,5" Content="Symbol"></Label>
                <TextBox Width="180"  Margin="5,5,5,5"></TextBox>
                <Label FontWeight="Bold" Margin="5,5,5,5" Content="Start Date"></Label>
                <DatePicker Width ="150"  Margin="5,5,5,5"></DatePicker>

                <Label FontWeight="Bold" Margin="5,5,5,5" Content="End Date"></Label>
                <DatePicker Width ="150" Margin="5,5,5,5"></DatePicker>

                <Button FontWeight="Bold" Width ="100" Margin="5,5,5,5" Content="Get History"></Button>
            </StackPanel>
            <StackPanel>
                <DockPanel>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                        <DataGrid IsReadOnly="True" ItemsSource ="{Binding HistoricStockPrices}" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding PriceAtDate}" Width ="100" Header="Date"/>
                                <DataGridTextColumn Binding="{Binding Symbol}" Width ="100" Header="Symbol"/>
                                <DataGridTextColumn Binding="{Binding Name}" Width ="150" Header="Name"/>
                                <DataGridTextColumn Binding="{Binding LastTradedPrice}" Width ="100" Header="Last Traded Price"/>
                                <DataGridTextColumn Binding="{Binding OpenPrice}" Width ="100" Header="Open Price"/>
                                <DataGridTextColumn Binding="{Binding TradedVolume}" Width ="100" Header="Traded Volume"/>
                                <DataGridTextColumn Binding="{Binding Bid}" Width ="100" Header="Bid Price"/>
                                <DataGridTextColumn Binding="{Binding Ask}" Width ="100" Header="Ask Price"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </DockPanel>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
